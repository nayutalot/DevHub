node_modules/
out/
dist/
*.log

# 机器本地/扫描器状态
.mimosa/
.zcode/

# Android 构建产物与机器本地配置
android/.gradle/
android/build/
android/app/build/
android/core/build/
android/local.properties
android/.kotlin/

# 证据日志例外：acceptance 下的 .log 是交付证据，必须追踪
!acceptance/**/*.log

# 生成物：mcp-acceptance 每次运行刷新（时间戳在内），追踪会弄脏工作树并使 A12 干净树断言自反
acceptance/mcp-scenario-report.json
# CP6 安装包备料独立输出目录（不替换在役 dist/win-unpacked）
dist-cp6/
